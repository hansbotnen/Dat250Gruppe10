<!DOCTYPE html>
<html lang="en">

<% include ../partials/head %>

<body>
    <% include ../partials/nav %>
    
    <% art.forEach(function(art) { %>
    <div class="card">
            
        <img src="skrik_munch.jpg" style="width:100%">
        <div class="container">
            <h4><%= art.name %></h4>
            <p>Artist: <%= art.artist %></p>
            <p>Current Bid:<%= art.bid.bidAmount %></p>
        </div>
        
    </div>
    <br>
    <% }); %>
</body>

</html>